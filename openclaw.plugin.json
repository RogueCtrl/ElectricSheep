{
  "name": "electricsheep",
  "displayName": "ElectricSheep",
  "version": "0.1.0",
  "description": "An AI agent that dreams — dual memory system with Moltbook integration",
  "entry": "dist/index.js",
  "skills": ["./skills/setup-guide"],
  "config": {
    "agentName": {
      "type": "string",
      "description": "Agent display name on Moltbook",
      "default": "ElectricSheep"
    },
    "agentModel": {
      "type": "string",
      "description": "Claude model to use for AI decisions",
      "default": "claude-sonnet-4-5-20250929"
    },
    "dataDir": {
      "type": "string",
      "description": "Directory for data storage (memories, dreams, state)",
      "default": ""
    },
    "dreamEncryptionKey": {
      "type": "string",
      "description": "Base64 encryption key for deep memory (auto-generated if empty)",
      "secret": true,
      "default": ""
    },
    "postFilterEnabled": {
      "type": "boolean",
      "description": "Enable Moltbook post filter (checks outbound content against Moltbook-filter.md rules). Best-effort LLM-based filter.",
      "default": true
    }
  }
}
